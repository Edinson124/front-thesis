export const reason = ['Falta de stock', 'Unidades incompatibles', 'Unidades próxima a vencer', 'Devolución de unidades'];

export const reasonOptions = reason.map((reason) => ({
  value: reason,
  label: reason
}));
